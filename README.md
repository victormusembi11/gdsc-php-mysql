# gdsc-php-mysql

## Login to MySQL

```bash
mysql -u root -p
```

You will be prompted for the password, by default there's no password.

## Run PHP Server

```bash
php -S localhost:8000
```

visit [[http://localhost:8000]]
